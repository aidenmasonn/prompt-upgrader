# Gemini — used for embeddings only (text-embedding-001, 3072 dimensions)
GEMINI_API_KEY=AIza...

# Anthropic — used for prompt generation (Claude Opus 4.6 with adaptive thinking)
ANTHROPIC_API_KEY=sk-ant-...

# Supabase — use the service role key (not anon) so API routes can bypass RLS
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
